# Google スプレッドシート版 — 記述・運用ガイド

## 全体像

このブックは、**プロダクトバックログ**をスプレッドシートで管理し、チームの優先度合意・スプリント計画・AI やエンジニアへの受け渡しに使うために作られている。

バックログの項目は、**誰が**（アクター）・**何をしたい**（要求）・**それはなぜか（価値）** の 3 点を必ず書くことを基本とする。

タブ構成は次の通り。

1. **バックログシート**（メニューでシート名を指定して追加）— 項目本体
2. 🔢 ID管理 — 🔢 ID管理 で各シートの採番管理（非表示）

シート名を変えてメニューを繰り返し実行すると、**1 つのスプレッドシートに複数のバックログ**（例：レセハブアプリ、管理コンソール）を持てる。ID は **シートごとに独立して** PBL-001 から採番される。

## 目次

**書き方**

- [タブの役割](#guide-tab-roles)
- [記述スタイル](#guide-writing-style)

**操作**

- [ID の追加と管理](#guide-id)
- [プルダウン](#guide-dropdowns-refresh)

---

# 書き方

<a id="guide-tab-roles"></a>

## タブの役割

| タブ名 | このシートで書くこと |
|--------|---------------------|
| バックログシート（名前を指定） | プロダクトバックログの項目一覧。ID・テーマ・ステータス・着手可能性・誰が・何をしたい・なぜ・ポイント・PRD・JIRA・備考 を 1 行 1 項目で管理する。 |
| 🔢 ID管理 | 各バックログシートの採番管理（最終発番）。手動で編集しない。 |

---

<a id="guide-writing-style"></a>

## 記述スタイル

- 「何をしたい」は **「〜したい」** の形で、ユーザー視点で書きます。
- 「それはなぜか（価値）」は、**その要求がなぜ必要なのか**（困りごと・防ぎたいこと・実現したい状態）を書きます。ここが書けない項目は、優先度を決める材料がありません。
- 「テーマ」はプロダクトや機能領域（例：レセハブアプリ、管理コンソール）を書きます。
- 「ステータス」は `Open → In Sprint → Done / Closed` の順で進めます。シート上では `Open`=無着色・`In Sprint`=**青**・`Done`=**緑**・`Closed`=**灰**で表示されます。
- 「着手可能性」は `Ready` / `Not Ready`。**詳細設計・前提条件・受け入れ条件の準備ができた項目**を `Ready` にします。シート上では `Ready` が**緑**・`Not Ready` が**黄**で表示されます。
- 「ポイント」はストーリーポイント（相対見積もり）を入れます。プルダウンはフィボナッチ数列（`1 / 2 / 3 / 5 / 8`）。未見積もりなら空欄にします。
- 「PRD」や「JIRA」は、関連するドキュメントやチケットのリンクを入れます。
- 「備考」は、補足メモ（前提・制約・関連情報など）を必要に応じて書きます。位置づけには影響しない自由記述です。
- ID は自動採番です。行を削除しても欠番を詰めたり番号を再利用したりしません。
    - すでに発行した ID のセル値は **書き換えず**、不要になったときはステータスで `Closed` にするか行を削除してください（参照の整合のため）。

---

# 操作

<a id="guide-id"></a>

## ID の追加と管理

- バックログシートのデータ行に直接入力すると、初めて内容が入った時点で **ID が自動採番**されます。
- ID は **シートごと独立**に `PBL-001` から採番されます（複数のバックログシートがあっても、それぞれ 001 から始まる）。
- 手で ID を直した・別ブックからコピーしたあとは、メニュー「バックログ」→「🔢 IDカウンタをブックから再同期」を実行すると IDカウンタを再同期できる。

---

<a id="guide-dropdowns-refresh"></a>

## プルダウン

- プルダウンは **ステータス**・**着手可能性**・**ポイント** の 3 つで、テンプレ展開時（メニューでシート追加時）にデータ行へ適用されます。
- ステータスは `Open → In Sprint → Done / Closed`（`In Sprint`=青・`Done`=緑・`Closed`=灰・`Open`は無着色に自動着色）、着手可能性は `Ready` / `Not Ready`（`Ready`=緑・`Not Ready`=黄に自動着色）、ポイントは `1 / 2 / 3 / 5 / 8` から選びます。
