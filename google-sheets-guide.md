# Google スプレッドシート版 記述・運用ガイド

## 全体像

このブックは、**プロダクトバックログ**をスプレッドシートで管理し、チームの優先度合意、スプリント計画、AI やエンジニアへの受け渡しに使うために作られている。
バックログの品質を保つための整理ガイドラインも、この文書にまとめてある。
バックログリファインメントやスプリント計画のたびに読み返して運用する。

バックログの項目は、**誰が**（アクター）、**何をしたい**（要求）、**それはなぜか（価値）** の 3 点を基本に書く。

タブ構成は次の通り。

1. **バックログシート**（メニューでシート名を指定して追加）：項目本体
2. **🔢 ID管理**（非表示）：各シートの採番管理

シート名を変えてメニューを繰り返し実行すると、**1 つのスプレッドシートに複数のバックログ**（例：レセハブアプリ、管理コンソール）を持てる。
ID は**シートごとに独立して** PBL-001 から採番される。

## 目次

**書き方**

- [項目の2種類（User Story / Enabler）](#guide-types)
- [User Story の書き方](#guide-user-story)
- [Enabler の書き方](#guide-enabler)
- [タブの役割](#guide-tab-roles)
- [記述スタイル（列ごとの書き方）](#guide-writing-style)

**品質と粒度**

- [品質チェック（INVEST）](#guide-invest)
- [大きすぎるストーリーの分割（SPIDR）](#guide-spidr)
- [Definition of Ready](#guide-dor)

**操作**

- [ID の追加と管理](#guide-id)
- [プルダウン](#guide-dropdowns-refresh)

---

# 書き方

<a id="guide-types"></a>

## 項目の2種類（User Story / Enabler）

バックログには User Story と Enabler の 2 種類があり、1 つのバックログに混在させて管理する。
区分は種別列で行う。

| 種別 | 目的 | 主語 | 価値の見え方 |
|------|------|------|--------------|
| User Story | エンドユーザーに直接価値を届ける | エンドユーザー（薬局管理者、スタッフ等） | ユーザーが直接見て触れる |
| Enabler | 機能開発を可能にする技術的基盤を作る | システム、開発チーム、プラットフォーム | 舞台裏で他のストーリーを支える |

Enabler は次の 4 タイプに分類する。

| プルダウンの値 | 説明 | 具体例 |
|----------------|------|--------|
| `Enabler:探索・検証` | 不確実性を減らす調査、プロトタイプ、スパイク | 技術選定調査、POC 作成 |
| `Enabler:アーキテクチャ` | システムアーキテクチャの構築・拡張 | API 設計、共通コンポーネント開発、リファクタリング |
| `Enabler:インフラ` | 開発・実行環境の整備 | CI/CD 構築、デリバリー基盤、テスト環境準備 |
| `Enabler:コンプライアンス・運用体制` | 規制・セキュリティ・法的要件への対応 | 脆弱性対応、セキュリティ監査対応 |

---

<a id="guide-user-story"></a>

## User Story の書き方

User Story は「**誰が**」「**何をしたい**」「**それはなぜか（価値）**」の 3 列で書く。
これは、次の 3 行テンプレートを 3 列に分解した形である。

```
As a <役割>, I want <機能>, so that <価値/理由>
```

- **誰が** = As a（役割）
- **何をしたい** = I want（「〜したい」の形、ユーザー視点）
- **それはなぜか（価値）** = so that（困りごと、防ぎたいこと、実現したい状態）

型を機械的に埋めるのではなく、「誰が」「何を実現したいか」「なぜそれが価値になるか」を会話で確認するための骨組みとして使う。
ストーリーは詳細な仕様書ではなく、対話のきっかけ（カード）として位置づける。
「それはなぜか（価値）」は優先度を決める材料である。
価値が自明な場合は省略してよいが、書けない項目は優先度を決める材料がない。

### 役割（誰が）の留意点

| 誰がに書くもの | 判定 | 対応 |
|----------------|------|------|
| エンドユーザー（薬局管理者、スタッフ等） | 最適 | そのまま |
| PdM（社内向け技術、運用改善） | 単独では弱い | 最終的な受益者や定量効果に書き換えるか、`Enabler` に分類する |
| PdM（本人が意思決定者として恩恵を受ける場合） | OK | 「それはなぜか（価値）」に定量的な意思決定価値を書く |
| システム（バッチ処理の内部ルール等） | NG | 「誰が」を具体的なシステム名（例：〇〇連携バッチ）にするか、`Enabler` に分類する |
| システム（別システムが API 呼び出し元の場合） | OK | 「〇〇連携バッチ」のように具体的なシステム名にする |

判定基準は、その役割を主語にしたときに「本当にその主体が価値を得ているか」「チームが優先順位判断できる情報が書かれているか」を確認すること。

---

<a id="guide-enabler"></a>

## Enabler の書き方

Enabler は `As a` 形式に無理に当てはめる必要はない。
テンプレでは次のように 3 列に分解して書く。

- **誰が**：システム、開発チーム、プラットフォーム（または具体的なシステム名）
- **何をしたい**：何を構築、調査、対応するか（例：デリバリー基盤（自動パッケージング、配布、ロールバック）を構築する）
- **それはなぜか（価値）**：どのユーザーストーリー、機能を支えるか、またはどの技術的課題を解決するか

Enabler には「どのユーザーストーリー、価値を支えるか」を必ず明記する。
Enabler は単独ではユーザー価値を生まないため、優先順位の根拠になる。

---

<a id="guide-tab-roles"></a>

## タブの役割

| タブ名 | このシートで書くこと |
|--------|---------------------|
| バックログシート（名前を指定） | プロダクトバックログの項目一覧。ID、テーマ、種別、ステータス、着手可能性、誰が、何をしたい、なぜ、ポイント、PRD、JIRA、備考 を 1 行 1 項目で管理する。 |
| 🔢 ID管理 | 各バックログシートの採番管理（最終発番）。手動で編集しない。 |

---

<a id="guide-writing-style"></a>

## 記述スタイル（列ごとの書き方）

- 「テーマ」：プロダクトや機能領域（例：レセハブアプリ、管理コンソール）を書く。
- 「種別」：`User Story` / `Enabler:探索・検証` / `Enabler:アーキテクチャ` / `Enabler:インフラ` / `Enabler:コンプライアンス・運用体制` から選ぶ。分類の詳細は「[項目の2種類](#guide-types)」を参照。
- 「ステータス」：`Open → In Sprint → Done / Closed` の順で進める。
- 「着手可能性」：`Ready` / `Not Ready`。[Definition of Ready](#guide-dor) を満たした項目のみ `Ready` にする。
- 「ポイント」：ストーリーポイント（相対見積もり）を入れる。プルダウンはフィボナッチ数列（`1 / 2 / 3 / 5 / 8`）。未見積もりなら空欄にする。
- 「PRD」「JIRA」：関連するドキュメントやチケットのリンクを入れる。
- 「備考」：補足メモ（前提、制約、依存関係、関連情報など）を必要に応じて書く。位置づけには影響しない自由記述。

---

# 品質と粒度

<a id="guide-invest"></a>

## 品質チェック（INVEST）

書いたストーリーが「良いストーリー」かどうかを、バックログリファインメントで以下の基準で確認する。

| 項目 | 意味 | チェックポイント |
|------|------|------------------|
| Independent（独立） | 他のストーリーに依存せず単独で価値がある | 依存関係があるなら依存先 ID を「備考」に明記 |
| Negotiable（交渉可能） | 詳細は固定せず、対話の中で調整できる | 仕様書のように固めすぎない |
| Valuable（価値がある） | ユーザーやビジネスへの価値が明確 | Enabler でも支える先の価値を書く |
| Estimable（見積り可能） | チームが規模感を見積れる程度に具体的 | 曖昧すぎる、大きすぎると見積り不能 |
| Small（小さい） | 1 スプリント内で完了できる粒度 | 大きければ SPIDR で分割 |

---

<a id="guide-spidr"></a>

## 大きすぎるストーリーの分割（SPIDR）

INVEST の Small を満たせない大粒度項目は、以下の 5 つの軸で分割する。
分割後の各ストーリーが単独で測定可能なビジネス価値を持つことを前提とし、単なるタスク分解にしない。

| 軸 | 分割の切り口 | 例 |
|------|--------------|-----|
| Spike | 仕様が固まっていない部分を調査タスクとして切り出す | 「デリバリー基盤の技術選定調査」 |
| Path | 処理の分岐（正常系、異常系、複数の業務パターン）ごとに分ける | 「正常系の ID 統合」「重複時の例外処理」 |
| Interface | 入力手段（画面、API、バッチ）ごとに分ける | 「画面からの設定変更」「API 経由の設定変更」 |
| Data | 対象データの種類、範囲ごとに分ける | 「一部薬局 ID のみ対応」「全件対応」 |
| Rules | ビジネスルールの複雑度で分ける | 「基本ルールのみ」「例外ルール対応」 |

分割したストーリーは、上位に Epic を置き、その配下にぶら下げる構造にする。
テンプレには Epic 専用の列がないため、親項目は「テーマ」を共通にしてまとめ、「備考」に親の ID や Epic 名を書く。

---

<a id="guide-dor"></a>

## Definition of Ready

「着手可能性」を `Ready` と判定するための基準。
以下のすべてを満たした項目のみ `Ready` とする。

- [ ] ストーリーが記述されている（User Story または Enabler 形式）
- [ ] テーマ、種別が設定されている
- [ ] 依存関係が特定され、blocking な依存は解消済み（依存先 ID は「備考」に記載）
- [ ] チームが見積り可能な粒度である（INVEST の Small、Estimable を満たす）
- [ ] ポイント見積りが実施されている

---

# 操作

<a id="guide-id"></a>

## ID の追加と管理

- バックログシートのデータ行に直接入力すると、初めて内容が入った時点で **ID が自動採番**される。
- ID は**シートごと独立**に `PBL-001` から採番される（複数のバックログシートがあっても、それぞれ 001 から始まる）。
- 行を削除しても欠番を詰めたり番号を再利用したりしない。
- すでに発行した ID のセル値は**書き換えず**、不要になったときはステータスで `Closed` にするか行を削除する（参照の整合のため）。
- 手で ID を直した、別ブックからコピーしたあとは、メニュー「バックログ」→「🔢 IDカウンタをブックから再同期」を実行すると ID カウンタを再同期できる。

---

<a id="guide-dropdowns-refresh"></a>

## プルダウン

- プルダウンは**種別**、**ステータス**、**着手可能性**、**ポイント**の 4 つで、テンプレ展開時（メニューでシート追加時）にデータ行へ適用される。
- 種別は `User Story` / `Enabler:探索・検証` / `Enabler:アーキテクチャ` / `Enabler:インフラ` / `Enabler:コンプライアンス・運用体制`（`Enabler` 系=薄紫に自動着色）、ステータスは `Open → In Sprint → Done / Closed`（`Open`=黄、`In Sprint`=青、`Done`=緑、`Closed`=灰に自動着色）、着手可能性は `Ready` / `Not Ready`（`Ready`=緑、`Not Ready`=黄に自動着色）、ポイントは `1 / 2 / 3 / 5 / 8` から選ぶ。
